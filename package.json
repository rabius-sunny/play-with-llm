{
  "name": "ecommerce-ai-chatbot",
  "scripts": {
    "dev": "bun run docker:up && bun run --hot src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:seed": "bun run src/db/seed.ts",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "check": "tsc --noEmit",
    "prepare": "husky"
  },
  "dependencies": {
    "@dqbd/tiktoken": "^1.0.21",
    "@modelcontextprotocol/sdk": "^1.17.1",
    "drizzle-orm": "^0.44.4",
    "gpt-3-encoder": "^1.1.4",
    "hono": "^4.8.10",
    "openai": "^5.10.2",
    "postgres": "^3.4.7",
    "zod": "^4.0.15"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.4",
    "husky": "^9.1.7",
    "typescript": "^5.9.2"
  }
}
